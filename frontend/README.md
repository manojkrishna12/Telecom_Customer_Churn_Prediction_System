# Frontend

React 18 + Vite (JavaScript, plain CSS).

```bash
cd frontend
npm install
npm run dev        # http://localhost:5173
```

The backend URL is read from `.env` (`VITE_API_URL`, default
`http://localhost:8000`). The backend must be running for predictions to work:
see the root README for backend startup instructions.
